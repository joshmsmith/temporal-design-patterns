[project]
name = "webhooks-python"
version = "0.1.0"
description = "Webhooks pattern demo — inbound webhook via Signal-with-Start (Python)"
requires-python = ">=3.12"
dependencies = [
    "temporalio==1.9.0",
]

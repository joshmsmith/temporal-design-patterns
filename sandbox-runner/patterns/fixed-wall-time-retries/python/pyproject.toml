[project]
name = "fixed-wall-time-retries-python"
version = "0.1.0"
description = "Fixed Wall-Time Retries pattern demo (Python)"
requires-python = ">=3.12"
dependencies = [
    "temporalio==1.9.0",
]
